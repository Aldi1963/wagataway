{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:prod": "pnpm run typecheck && pnpm --filter @workspace/wa-gateway run build && pnpm --filter @workspace/api-server run build",
    "deploy:prod": "pnpm install --frozen-lockfile && pnpm --filter @workspace/scripts run deploy:prod",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
